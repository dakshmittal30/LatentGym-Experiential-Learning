# The full trajectory data (data/trajectories/, ~186 MB) and figures are
# committed so anyone cloning the repo can run the site directly.
# Regenerate them with scripts/build_data.py and scripts/export_figures.py.

# OS / editor cruft
.DS_Store
*.swp
__pycache__/
*.pyc
