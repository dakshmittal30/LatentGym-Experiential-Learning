# Generated trajectory data (regenerable via scripts/build_data.py).
# The heavy per-trajectory JSON is NOT committed; index.json IS committed.
data/trajectories/

# Exported figures are regenerable via scripts/export_figures.py, but we keep
# them committed so the site renders without the source repo. Comment the next
# line out if you prefer to commit images.
# assets/img/

# OS / editor cruft
.DS_Store
*.swp
__pycache__/
*.pyc
